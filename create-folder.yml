---
- name: Create Folder
  hosts: servers
  tasks:
    - name: create dir task
      file:
        path: "{{ item }}"
        state: directory
        mode : '0755'
      loop:
        - /home/
        - /home/isofh/

      
