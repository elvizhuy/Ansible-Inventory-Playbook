---
- name: Change User Password
  hosts:
    databases:
      - db_jira_master
  become: yes

  tasks:
    - name: Change user password
      user:
        name: isofh
        update_password: always
        password: "{{ new_password }}"
      vars:
        new_password: 'Huynn@9890'